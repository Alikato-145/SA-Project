# B4 View Data

No new persistence is added. The pages render existing work-day, leave, overtime, advance, loan/installment, and debt response DTOs. Employee ID is a scoped query filter, not an authorization grant. Decimal values stay strings through forms and API calls.
